# ── Sensitive / large source material (NEVER commit) ──────────────
# Recordings, audio & screenshots of the existing live system — may contain real PHI.
videos old/
audio/
*.mp4
*.mov
*.mp3
*.aac
*.wav
frames/

# Claude: share project hooks/settings; keep machine-local & transient state out
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/settings.local.json
.claude/.security-review-pending

# ── Secrets & local config ────────────────────────────────────────
*.env
.env*
!*.env.example
appsettings.*.local.json
appsettings.Development.json
secrets.json
*.pfx
*.key

# ── .NET (ASP.NET Core backend) ───────────────────────────────────
bin/
obj/
*.user
.vs/
[Dd]ebug/
[Rr]elease/
artifacts/

# ── Node / React (frontend) ───────────────────────────────────────
node_modules/
dist/
build/
.vite/
coverage/
*.log
npm-debug.log*
pnpm-debug.log*

# ── OS / IDE ──────────────────────────────────────────────────────
Thumbs.db
.DS_Store
.idea/
*.swp

# Playwright MCP scratch output
.playwright-mcp/
